// T.C. = O(n)    S.C. = O(1)
class Compute {
    
    public int largest(int arr[], int n)
    {
        int i, largest = 0;
        for(i=0; i<n; i++){
            if(arr[i] > largest)
                largest = arr[i];
        }
        return largest;
    }
}
